import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function HabitsPage() {
  return (
    <PlaceholderPage
      title="Habits"
      description="Habit tracking with streaks, weekly consistency, and forgiving metrics."
      phase="3"
    />
  );
}
